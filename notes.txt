Cron_wa

CREATE TABLE `tabel_antrean_wa` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `no_hp` varchar(20) NOT NULL,
  `pesan` text NOT NULL,
  `status` enum('pending','terkirim','gagal') DEFAULT 'pending',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
);

--
-- Struktur dari tabel `surat_panggilan`
--

CREATE TABLE `surat_panggilan` (
  `id_surat_panggilan` int(11) NOT NULL,
  `nisn` text NOT NULL,
  `siswa_id` int(11) NOT NULL,
  `terakhir_dipanggil` date NOT NULL,
  `keterangan` text NOT NULL,
  `status_sp` int(1) NOT NULL,
  `last_updated` timestamp NOT NULL DEFAULT current_timestamp() ON UPDATE current_timestamp()
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

ALTER TABLE `app_setting` 
ADD COLUMN `tema_aplikasi` VARCHAR(50) NOT NULL DEFAULT 'default' AFTER `wa_blast`;

-- absen_mandiri geolocation
ALTER TABLE absen 
ADD COLUMN lat_masuk VARCHAR(50) NULL AFTER is_geolocation,
ADD COLUMN long_masuk VARCHAR(50) NULL AFTER lat_masuk,
ADD COLUMN photo_masuk VARCHAR(255) NULL AFTER long_masuk,
ADD COLUMN lat_pulang VARCHAR(50) NULL AFTER photo_masuk,
ADD COLUMN long_pulang VARCHAR(50) NULL AFTER lat_pulang,
ADD COLUMN photo_pulang VARCHAR(255) NULL AFTER long_pulang;

-- face recognition
ALTER TABLE siswa ADD COLUMN face_descriptor TEXT NULL;
ALTER TABLE guru ADD COLUMN face_descriptor TEXT NULL;
ALTER TABLE pegawai ADD COLUMN face_descriptor TEXT NULL;